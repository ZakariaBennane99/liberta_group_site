/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  basePath: '/liberta_group_site', // Replace 'your-repo-name' with the name of your GitHub repository
  assetPrefix: '/liberta_group_site/', // Replace 'your-repo-name' with the name of your GitHub repository
};
